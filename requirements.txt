# Requirements for Bot Negociador — PLN Project
# Compatible con uv: https://github.com/astral-sh/uv
#
# Instalar con uv:
#   uv pip install -r requirements.txt
#
# O crear venv + instalar:
#   uv venv && source .venv/bin/activate && uv pip install -r requirements.txt

# ── HTTP (API del juego) ─────────────────────────────────────────────────
requests>=2.32.5

# ── Cliente de Ollama (IA local) ─────────────────────────────────────────
ollama>=0.6.1

# ── CLI moderna ──────────────────────────────────────────────────────────
click>=8.3.1

# ── Interfaz de terminal enriquecida ─────────────────────────────────────
rich>=14.3.2

# ── Logging estructurado ────────────────────────────────────────────────
loguru>=0.7.3

# ── Validación de datos ─────────────────────────────────────────────────
pydantic>=2.12.5
